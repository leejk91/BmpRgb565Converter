<Window x:Class="BmpRgb565Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BmpRgb565Viewer"
        mc:Ignorable="d"
        Title="BMP RGB565 Viewer" Height="600" Width="1000" MinHeight="500" MinWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="8">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" HorizontalAlignment="Left">
                <Button x:Name="OpenButton" Content="열기 (BMP)" Padding="12,6" Margin="0,0,8,0" Click="OnClickOpen"/>
                <Button x:Name="ConvertButton" Content="RGB565 변환" Padding="12,6" Margin="0,0,8,0" Click="OnClickConvert"/>
                <Button x:Name="SaveButton" Content="저장 (RGB565 RAW .bin)" Padding="12,6" Click="OnClickSave"/>
                <Button x:Name="SaveRleButton" Content="저장 (RGB565 RLE)" Padding="12,6" Margin="8,0,0,0" Click="OnClickSaveRle"/>

            </StackPanel>
            <StackPanel Orientation="Vertical" VerticalAlignment="Center" Width="470">
                <CheckBox x:Name="BigEndianCheckBox" Content="빅엔디안(기본값:리틀)" Margin="10,0,16,0"/>
                <CheckBox x:Name="RLECheckBox" Content="라인 기반 RLE(기본값:글로벌)" Margin="10,0,16,0"/>
                <TextBlock x:Name="StatusText" Text="준비됨" VerticalAlignment="Center" TextAlignment="Right"/>
            </StackPanel>
            
        </DockPanel>

        <Grid Grid.Row="1" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" BorderBrush="#DDD" BorderThickness="1" Padding="8">
                <DockPanel>
                    <TextBlock Text="원본 (BMP)" FontWeight="Bold" Margin="0,0,0,8"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Image x:Name="OriginalImage" Stretch="None"/>
                    </ScrollViewer>
                </DockPanel>
            </Border>
            <Grid Grid.Column="1"/>
            <Border Grid.Column="2" BorderBrush="#DDD" BorderThickness="1" Padding="8">
                <DockPanel>
                    <TextBlock Text="변환 (RGB565)" FontWeight="Bold" Margin="0,0,0,8"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Image x:Name="ConvertedImage" Stretch="None"/>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
